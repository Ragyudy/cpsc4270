CPSC 4270 – Programming Assignment 1
NetID: gy236

This submission contains:
- Card.hpp / Card.cpp: Modified to use 0-based indexing, removed "bad", switched to array and .at() for runtime checking.
- Deck.hpp / Deck.cpp: Implements a Spider deck of 104 cards, shuffle(), and print() to output 13 cards per line.
- main.cpp: Test driver. Reads seed from command line, prints initial deck, shuffles, and prints shuffled deck. Uses banner() and bye() from tools library.
- tools.hpp / tools.cpp: Provided utility library (unchanged except included here for completeness).

Command to compile and clean executable files:
make, make clean

User just needs to provide a seed as a command line argument to run the program.
e.g., ./main 42